<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hritik.articlereader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/articlereader"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/articlereader_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_noactionbar">
        <activity
            android:name=".Hamburger_menu"
            android:label="@string/title_activity_hamburger_menu"
            android:theme="@style/AppTheme_noactionbar.NoActionBar"></activity>
        <activity android:name=".Main_hamburger" />
        <activity
            android:name=".Bottom_navbar"
            android:label="@string/title_activity_bottom_navbar" />
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".Signup" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />'
    </application>

</manifest>